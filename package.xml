<?xml version="1.0"?>
<package format="3">
  <name>elevation_traversability</name>
  <version>2.0.0</version>
  <description>
    Elevation-based traversability analysis using grid_map.
    SLAM-agnostic ROS 2 Jazzy port. Subscribes to a registered point
    cloud, constructs an elevation map, computes terrain features
    (slope, roughness, normals, step height), and publishes
    traversability grid maps and occupancy grids.
    Works with any SLAM/odometry source that publishes a configurable
    map_frame -> robot_frame TF.
  </description>
  <maintainer email="mohameddbardana@gmail.com">Mohamed Bardana</maintainer>
  <license>BSD-3-Clause</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>

  <depend>rclcpp</depend>
  <depend>sensor_msgs</depend>
  <depend>nav_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>std_msgs</depend>
  <depend>std_srvs</depend>
  <depend>tf2</depend>
  <depend>tf2_ros</depend>
  <depend>tf2_geometry_msgs</depend>
  <depend>pcl_conversions</depend>
  <depend>grid_map_core</depend>
  <depend>grid_map_ros</depend>
  <depend>grid_map_msgs</depend>
  <depend>grid_map_cv</depend>
  <depend>eigen</depend>
  <depend>libpcl-all-dev</depend>
  <depend>libopencv-dev</depend>

  <exec_depend>rosidl_default_runtime</exec_depend>
  <member_of_group>rosidl_interface_packages</member_of_group>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
